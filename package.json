{
  "name": "self-improve-cli",
  "version": "0.1.0",
  "description": "Lightweight self-improving agentic coding CLI prototype.",
  "license": "MIT",
  "bin": {
    "sicli": "bin/self-improve-cli.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "npm test",
    "postinstall": "echo '✅ sicli installed! Run \"sicli init\" to setup workspace or \"sicli tui\" to launch TUI.'"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "blessed": "^0.1.81"
  }
}
