---
title: "Handoff: Permission Modes"
date: 2026-04-26
session_id: 006-permission-modes
type: handoff
---

# Handoff: Permission Modes

## Session Summary

Implemented four permission modes for tool execution: `secure`, `partial_secure`, `ai_reviewed`, and `auto_approve`. Added CLI and chat controls, git reversibility checks, and clean-context AI review for action tools. Follow-up fix: `ai_reviewed` now reviews profile-`ask` tools instead of asking immediately, and old base profiles get default `write_file` policy backfilled at load time.

## Specs Created

- `spec/006.permission-modes.md`

## Specs Changed

- `spec/006.permission-modes.md` moved to `IMPLEMENTED` and updated with implementation/validation notes.

## Files Modified

- `src/config.js`
- `src/agent.js`
- `bin/self-improve-cli.js`
- `test/config-tools.test.js`
- `README.md`
- `spec-driven-llm-wiki/spec/006.permission-modes.md`
- `spec-driven-llm-wiki/wiki/components/agent-chat-loop.md`
- `spec-driven-llm-wiki/wiki/log.md`

## Graph Updates

- Graph not rebuilt because `spec-driven-llm-wiki/tools` dependencies are unavailable: `tsx` command missing.
- Existing `[[components/agent-chat-loop]]` page updated.

## Wiki Updates

- Updated agent chat loop component.
- Updated wiki log.

## Validation

- `npm test` passed: 25 tests.
- `node bin/self-improve-cli.js permissions` passed.
- `node bin/self-improve-cli.js permissions secure` passed.
- `node bin/self-improve-cli.js permissions partial_secure` passed.
- Manual required-section check passed in final validation batch.
- `cd spec-driven-llm-wiki/tools && npm run validate-spec -- --all` failed because `tsx` is not installed.

## Open Questions

- Whether `partial_secure` should allow common test commands without approval.
- Whether `ai_reviewed` should use a cheaper reviewer model setting separate from the main model.

## Next Steps

1. Live-test all four modes in interactive chat.
2. Add `/permissions explain` with examples.
3. Add optional separate reviewer provider/model config.
